<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="NftHigherOrLowerGame.Pages.GameOverPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="GameOverPage">
    <Shell.BackButtonBehavior>
        <BackButtonBehavior IsEnabled="False" IsVisible="False" />
    </Shell.BackButtonBehavior>
    <VerticalStackLayout
        HorizontalOptions="Center"
        Spacing="10"
        VerticalOptions="Center">
        <Label
            FontSize="24"
            HorizontalOptions="Center"
            Text="Game Over"
            VerticalOptions="Center" />
        <Label
            x:Name="PointsLabel"
            FontSize="14"
            HorizontalOptions="Center"
            VerticalOptions="Center" />
        <Label
            x:Name="CorrectLabel"
            FontSize="14"
            HorizontalOptions="Center"
            VerticalOptions="Center" />
        <Label
            x:Name="WrongLabel"
            FontSize="14"
            HorizontalOptions="Center"
            VerticalOptions="Center" />
        <Label
            x:Name="TotalLabel"
            FontSize="14"
            HorizontalOptions="Center"
            VerticalOptions="Center" />
        <Button Clicked="HomeButton_Clicked" Text="Return Home" />
    </VerticalStackLayout>
</ContentPage>